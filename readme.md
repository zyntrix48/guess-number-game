# Угадай число

Простая консольная игра "Угадай число", где компьютер загадывает число в заданном диапазоне, а игрок пытается его отгадать.

## 🎮 Описание игры

Программа генерирует случайное число в указанном вами диапазоне. Ваша задача - угадать это число, получая подсказки "Больше!" или "Меньше!" после каждой попытки.

## 🚀 Установка и запуск

1. Убедитесь, что у вас установлен Python 3.6+
2. Скачайте файлы проекта
3. Запустите игру:
```bash
python main.py
```

## 📝 Как играть

1. Программа запросит минимальное и максимальное число для диапазона
2. Компьютер загадает случайное число в этом диапазоне
3. Вводите числа, пытаясь угадать загаданное
4. Получайте подсказки:
   - `"Больше!"` - если ваше число меньше загаданного
   - `"Меньше!"` - если ваше число больше загаданного
   - `"Вы угадали число!"` - при правильном ответе

### Пример игры:
```
Введите минимальное число: 1
Введите максимальное число: 100
Введите предполагаемое число: 50
Меньше!
Введите предполагаемое число: 25
Больше!
Введите предполагаемое число: 37
Вы угадали число!
```

## 🗂️ Структура проекта

```
guess_number/
├── main.py                     # Основной файл игры
├── modules/
│   ├── generate_number.py      # Модуль генерации случайных чисел
│   └── guess_controller.py     # Модуль проверки догадок
└── README.md                   # Документация
```

## 🔧 Модули

### `main.py`
- Запускает игровой цикл
- Обрабатывает пользовательский ввод
- Координирует работу между модулями

### `modules/generate_number.py`
```python
import random

def generate_random_number(min_num, max_num):
    """
    Генерирует случайное число в диапазоне [min_num, max_num]
    """
    return random.randint(min_num, max_num)
```

### `modules/guess_controller.py`
```python
def guess_number(user_guess, target_number):
    """
    Сравнивает предположение пользователя с загаданным числом
    Возвращает подсказку или сообщение о победе
    """
    if user_guess == target_number:
        return "Вы угадали число!"
    elif user_guess > target_number:
        return "Меньше!"
    else: 
        return "Больше!"
```

## ⚡ Особенности

- ✅ **Валидация диапазона** - корректная обработка минимального и максимального чисел
- ✅ **Бесконечный игровой цикл** - игра продолжается до победы
- ✅ **Четкие подсказки** - помогают быстрее найти число
- ✅ **Модульная архитектура** - легко расширять функциональность
- ✅ **Обработка целых чисел** - работа с целочисленными значениями

## 🎯 Стратегия игры

Для наиболее эффективного угадывания используйте **метод бинарного поиска**:
- Начинайте со среднего числа диапазона
- Каждой следующей попыткой делите оставшийся диапазон пополам
- Так вы найдете число за минимальное количество попыток!
  
---

*Удачи в игре! Запустите `python main.py` чтобы начать.*
